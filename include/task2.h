#include <iostream>
#include <cmath>
#include <algorithm>

template <typename T, typename T2>
double averageArr(T* a, T2 b)
{
	T summa = 0;
	for (unsigned int i = 0; i < b; i++) {
		sum += *(a + i);
	}
	answer = (double)(sum / b);
	return answer;
}

